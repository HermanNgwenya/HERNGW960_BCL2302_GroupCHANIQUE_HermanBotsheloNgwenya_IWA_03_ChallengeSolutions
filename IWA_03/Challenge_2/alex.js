// alex.js

const firstname = "Alex";
const surname = "Naidoo";
export const roleAlex = "Head of Marketing";

const display= firstname + " " + surname + " (" + roleAlex + ")";
document.querySelector('#alex').innerText = display;








/** SOLUTIONS
 * I declared the variables using the const function
 * I exported the 'roleAlex' variable
 * I delared the display variable
 */